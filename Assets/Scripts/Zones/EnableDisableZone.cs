using System;
using System.Collections;
using System.Collections.Generic;
using Player.Observer_pattern;
using UnityEngine;

public class EnableDisableZone : MonoBehaviour
{
    // // Start is called before the first frame update
    // private ZoneCleaner _zoneCleaner;
    // public PlayerActions currentZone;
    // [SerializeField] private GameObject level;
    //
    // void Start()
    // {
    //     level.SetActive(false);
    //     _zoneCleaner = FindObjectOfType<ZoneCleaner>();
    //     _zoneCleaner.AddObserver(this);
    // }
    //
    // public void OnNotify(PlayerActions playerAction)
    // {
    //     if (playerAction == currentZone) level.SetActive(true);
    // }
}